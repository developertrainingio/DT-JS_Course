//Creating strings


//Escape characters \


//Using .length
const passphrase = 'Open the door!';
console.log(passphrase.length); 
// can use .length for verifying that length != 0 (meaning the field is not empty)

//Using toLowerCase()
console.log(passphrase.toLowerCase());
console.log(passphrase); // the method doesn’t actually change the variable
